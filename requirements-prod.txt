# ═══════════════════════════════════════════════════════════════════════════
# RAG Knowledge Chatbot - PRODUCTION (Render-optimized, memory-efficient)
# Lightweight version for free tier deployments
# ═══════════════════════════════════════════════════════════════════════════

# Document Processing
pypdf>=4.0.0
python-docx>=1.0.0
beautifulsoup4>=4.12.0

# Web & API
requests>=2.30.0
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# Data Validation
pydantic>=1.10.0

# Configuration
python-dotenv>=1.0.0

# Utilities
tqdm>=4.60.0
typing-extensions>=4.0.0
numpy>=1.24.0
scikit-learn>=1.0.0

# LLM API
anthropic>=0.30.0

# NOTE: Removed for production:
# - torch (huge, 2GB+)
# - sentence-transformers (requires torch)
# - SQLAlchemy/psycopg2 (not needed for Render)
# - Development packages (black, pylint, pytest, etc.)
#
# Using in-memory embeddings cache instead of re-computing
